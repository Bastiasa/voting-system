import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'votador.regionalsimonbolivar.ie.edu.co',
  appName: 'Votador IE RSB',
  webDir: 'dist-react'
};

export default config;
